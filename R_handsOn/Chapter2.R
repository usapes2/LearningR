#Project 2 Playing Cards
int <- c(1L,2L) # L after number indicates to R that this is an integer
text <- c("ace","black")
# typeof askes what type of object we are dealing with
die<-1:6
# integer
typeof(die)
die <-c(1,2,3,4,5,6)
typeof(die)
# double

# Precision examples
sqrt(2)^2 - 2
#suppose to be zero